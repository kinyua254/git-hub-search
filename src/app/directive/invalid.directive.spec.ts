import { InvalidDirective } from './invalid.directive';

describe('InvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidDirective();
    expect(directive).toBeTruthy();
  });
});
